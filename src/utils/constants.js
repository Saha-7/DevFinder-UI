// Dynamic URL
// export const BASE_URL = location.hostname === "localhost"? "http://localhost:3000" : "/api";

//Local URL

// export const BASE_URL="http://localhost:3000"


export const BASE_URL = location.hostname === "localhost"
  ? "http://localhost:3000" 
  : "https://devfinder-4ah0.onrender.com";
